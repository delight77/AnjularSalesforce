#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsforce-metadata-tools/bin/jsforce-deploy" "$@"
  ret=$?
else 
  node  "$basedir/../jsforce-metadata-tools/bin/jsforce-deploy" "$@"
  ret=$?
fi
exit $ret
